# Language setting
language: java

# Version number
jdk:
  - openjdk11


build:
  ci:
    - java --version
    - javac --version
    - source /etc/drydock/.env
    - java --version
    - javac --version
